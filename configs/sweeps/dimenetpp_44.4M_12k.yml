includes:
- configs/s2ef/12k/base.yml

model:
  name: dimenetplusplus
  hidden_channels: 512
  out_emb_channels: 384
  num_blocks: 3
  cutoff: 6.0
  num_radial: 6
  num_spherical: 7
  num_before_skip: 6
  num_after_skip: 12
  num_output_layers: 20
  regress_forces: True
  use_pbc: True
  int_emb_size: 64

optim:
  batch_size: 2
  eval_batch_size: 1
  num_workers: 64
  lr_initial: 0.0001
  lr_gamma: 0.1
  lr_milestones: # epochs at which lr_initial <- lr_initial * lr_gamma
    - 5
    - 10
    - 15
  warmup_epochs: 1
  warmup_factor: 0.2
  max_epochs: 1
  energy_coefficient: 0
  force_coefficient: 100
